apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: workshop
    component: parksmap
    createdBy: parksmap-template
    role: frontend
  name: parksmap
spec:
  port:
    targetPort: 8080-tcp
  to:
    kind: Service
    name: parksmap
    weight: 100
  wildcardPolicy: None

